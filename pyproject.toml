[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lesson38_app"
version = "0.1.0"
description = "Aplikacja z lekcji 38"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests",
]